#pragma once

#include <vector>

using std::vector;

// make a map of char tile, string enemy name
// search the enemy name in a suffix trie, node will correspond to what type of enemy it is (say a human warrior)
// function will return a unique pointer to the constructed specific enemy object
// then for each generic enemy in the enemy vector we set the generic enemy pointer to the returned specific enemy pointer

struct LevelOneImpl {
	
};
